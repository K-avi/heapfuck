heapfuck isn't very ambitious ; 

I'll reuse code from (SIASL)² and siasl ; 


from siasl2 :  ast.c ; exec.c ; cmdline_interp.c

from siasl : stack.c ; globals.c 


might also add some operators like something to pop a value from the heap


-> to add will use the "read" instruction of bf , instead of reading value and set curcell to it 
    will append to heap 

-> for pop will use "!" which will pop curindex 

-> also might implement "%" to print as int

heap manipulation written ; not tested yet though 